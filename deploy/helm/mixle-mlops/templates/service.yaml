apiVersion: v1
kind: Service
metadata:
  name: {{ include "mixle-mlops.fullname" . }}
  labels:
    {{- include "mixle-mlops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "mixle-mlops.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
